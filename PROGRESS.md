# Активная задача: P8 — отображение квоты в профиле веб-фронта

Ветка: `feat/quota-profile-ui`

**Цель.** Пользователь видит свой остаток генераций в интерфейсе.

**Суть.** Раздел в профиле/настройках `servers/nextjs`, читающий endpoint
из P4 (`GET /api/v1/user/quota`). Зависимость P4 закрыта.

P7 (ретраи вебхуков) закрыта — см. `docs/progress/P7-webhook-retries.md`.
